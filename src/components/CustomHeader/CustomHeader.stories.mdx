import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { createForm, createBuilder } from "../../utils";

<Meta title="Components/CustomHeader" />

# CustomHeader

This is a demo component, will be removed before release to production.

## Renderer

Just a presentation component.

<Canvas>
  <Story name="Renderer">
    {() =>
      createForm({
        form: {
          components: [
            {
              type: "customheader",
              key: "customheader",
              tag: "h1",
              content: "This is a custom header",
            },
          ],
        },
      })
    }
  </Story>
</Canvas>

## Builder

This component can be drag and drop from the Form.io builder sidebar `custom` group.
You can preview the component's settings in the example below.

<Canvas>
  <Story name="Builder">
    {() =>
      createBuilder({
        form: {
          components: [
            {
              type: "customheader",
              key: "customheader",
              tag: "h1",
              content: "This is a custom header",
            },
          ],
        },
      })
    }
  </Story>
</Canvas>
